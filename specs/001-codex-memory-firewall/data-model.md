# Data Model: Codex Memory Firewall

**Feature**: `001-codex-memory-firewall`

The event ledger is authoritative. Candidate, quarantine, active-memory, policy,
and statistics tables are rebuildable projections or operational indexes unless
explicitly noted.

## Identifiers and Common Types

- IDs use lowercase UUIDv7 strings when generated by Verity. Contracts accept a
  broader bounded identifier grammar so external Codex IDs remain opaque; that
  compatibility grammar does not weaken Verity's own generation rule.
- Runtime-produced times are timezone-aware UTC values. External API date-time
  inputs may carry an explicit offset and are normalized before persistence.
  Signed envelope timestamps are always serialized as
  `YYYY-MM-DDTHH:MM:SS[.ffffff]Z`: omit the fraction when zero and otherwise
  remove trailing fractional zeroes.
- Digests are lowercase 64-character SHA-256 hexadecimal strings.
- Signatures and raw public keys use standard padded Base64 strings.
- Enumerations are serialized as lowercase snake-case strings except published
  event type names and Codex hook names.
- Raw evidence is never used as an identifier and never appears in telemetry.

## Evidence

Evidence records the source observation from which zero or more candidates were
derived.

| Field | Type | Rule |
|---|---|---|
| `evidence_id` | UUIDv7 | Primary identifier. |
| `session_id` | string | Required, opaque identifier, 8-128 characters. |
| `task_id` | string/null | Opaque identifier, 8-128 characters when present. |
| `source_class` | enum | `user_input`, `tool_output`, `agent_output`, `imported_file`, `prior_memory`, `compaction`, `session_summary`, `external_event`. Operator actions and system events use event-envelope source classes rather than candidate evidence classes. |
| `source_name` | string/null | Pattern-sanitized tool/source label. URL-like input is reduced to `url.<hostname>`; otherwise query and fragment suffixes are removed before bounded safe-character normalization and signed persistence. |
| `safe_excerpt` | string | Pattern-sanitized representation, bounded to 2,000 characters and permanently bound by `EvidenceCaptured`. It is not guaranteed non-sensitive. |
| `content_digest` | digest | SHA-256 over the exact original submitted bytes, which are not retained by the MVP. |
| `content_size` | integer | Non-negative byte count. |
| `retention_state` | enum | `digest_only`, `protected_local`, `expired`. |
| `captured_at` | UTC time | Clock-provided. |
| `metadata` | object | Bounded safe scalar values only. |

The MVP writes `retention_state=digest_only`: original submitted bytes are not
stored. The signed capture payload and evidence projection still retain the
bounded sanitized excerpt. Pattern-based sanitization is not exhaustive, so an
unrecognized sensitive value can remain in that excerpt; the local ledger and
backups must be handled as sensitive. Demo mode uses synthetic content only.

## Pending Evidence Queue

The hook-facing API acknowledges only after `EvidenceCaptured` and one bounded
queue row commit atomically. This operational queue lets synchronous Codex hooks
return before candidate extraction or semantic assessment.

| Field | Type | Rule |
|---|---|---|
| `evidence_id` | UUIDv7 | Primary key and reference to the signed capture record. |
| `state` | enum | `pending` or `failed`. |
| `sanitized_content` | string/null | Full pattern-sanitized evidence while pending; null after terminal failure. |
| `sanitized_content_digest` | digest | Must match both the queued text and the digest bound in `EvidenceCaptured.metadata`. |
| `enqueued_at` | UTC time | Must equal the signed capture time. |
| `attempts` | integer | Non-negative evaluation-attempt count. |
| `next_attempt_at` | UTC time | Due time for bounded retry. |
| `last_error_code` | string/null | Content-free queue/evaluation class. |
| `failed_at` | UTC time/null | Required only in terminal failed state. |
| `terminal_event_id` | UUIDv7/null | Required only in terminal failed state; references `EvidenceEvaluationFailed`. |

Default admission limits are 256 pending items and 16 MiB of pending sanitized
text. Evaluation receives at most three attempts and one hour of queue age;
retry delay grows exponentially and is capped at five minutes. Successful
outcome events and queue deletion commit together. A zero-candidate drain
appends `EvidenceEvaluationCompleted` so successful consumption remains signed
and auditable. A terminal evaluation error
appends `EvidenceEvaluationFailed` and purges the full sanitized text while
retaining content-safe metadata and its digest. A digest mismatch is also a
ledger-health failure and disables new commits and injection. When a signed
capture remains usable, the queue worker appends an exact-schema
`EvidenceEvaluationFailed` terminal event with `queue_integrity_error`, purges
the queued body, and links the failed row to that event. Verification binds the
terminal failed row to the signed capture and failure events. The resulting
integrity state remains fail-closed after restart; deleting or weakening the
operational marker is not a supported recovery procedure.

## Memory Candidate

| Field | Type | Rule |
|---|---|---|
| `candidate_id` | UUIDv7 | Primary identifier. |
| `namespace` | string | Dot-separated lowercase segments; max 160 characters. |
| `kind` | enum | `fact`, `user_preference`, `project_convention`, `operational_instruction`, `tool_observation`, `task_summary`, `identity_assertion`, `policy_statement`, `credential_material`, `unknown`. |
| `statement` | string | Atomic sanitized statement, 1-8,192 characters. |
| `source_class` | enum | Copied from evidence and independently policy-visible. |
| `source_refs` | list[CandidateEvidenceRef] | At least one `{evidence_id, evidence_digest}` reference. Event envelopes use the distinct `{evidence_id, digest}` `EventEvidenceReference` shape. |
| `session_id` | string | Originating session. |
| `task_id` | string/null | Originating task. |
| `confidence` | decimal | 0 through 1 inclusive. |
| `durability_rationale` | string | Bounded explanation of future usefulness; live model output is pattern-sanitized again before signed persistence. |
| `persistence_requested` | boolean | Whether source explicitly requested durable reuse. |
| `authority_signal` | enum | `none`, `implied`, `explicit`, or `unknown`; advisory extractor observation only. |
| `secrecy_signal` | enum | `none`, `implied`, `explicit`, or `unknown`; records concealment language without granting authority. |
| `sensitivity` | enum | `public`, `internal`, `sensitive`, `restricted`, `credential`. |
| `requested_ttl_seconds` | integer/null | 60 through 31,536,000 seconds and policy-capped. |
| `contains_redactions` | boolean | True when local sanitization replaced protected content. |
| `extractor_provider` | enum | `recorded_fixture`, `live_openai`, `live_codex_subscription`, `deterministic`. |
| `extractor_version` | string | Required version/prompt tuple. |
| `content_digest` | digest | SHA-256 binding of the sanitized statement. |
| `created_at` | UTC time | Candidate creation time. |

### Namespace rules

- Supported roots: `facts`, `preferences`, `project`, `instructions`,
  `policies`, `tool_results`, `scratch`, `credentials`.
- `policies.*` and configured protected namespaces cannot be changed by user,
  agent, or tool sources under the default policy.
- `credentials.*` and `credential_material` are blocked under the default
  policy; no raw credential becomes memory.
- Operational instructions and policy statements require stronger source trust
  and review than facts, observations, or summaries.

## Detector Result

| Field | Type | Rule |
|---|---|---|
| `result_id` | UUIDv7 | Primary identifier. |
| `candidate_id` | UUIDv7 | Required reference. |
| `detector_id` | string | Stable entry-point identifier. |
| `detector_version` | string | Required semantic or content version. |
| `execution_order` | integer | Non-negative deterministic aggregation index. |
| `status` | enum | `ok`, `timeout`, `error`, `cancelled`, `malformed`. |
| `matched` | boolean/null | Boolean only when status is `ok`; null on failure. |
| `severity` | enum | `info`, `low`, `medium`, `high`, `critical`. |
| `confidence` | decimal | 0 through 1 inclusive. |
| `categories` | ordered list[string] | Unique safe category names. |
| `message` | string | Content-free or redacted explanation. |
| `evidence_offsets` | list[SafeOffsetRef] | Offsets into sanitized text or digest refs. |
| `metadata` | object | Bounded safe scalars; raw matches prohibited. |
| `failure_class` | string/null | Null when status is `ok`; a safe error class otherwise. |
| `latency_ms` | integer | Non-negative. |
| `recorded_at` | UTC time | Required. |

Aggregation sorts by `(detector_id, detector_version, result_id)` before policy
evaluation. Duplicate detector IDs fail discovery rather than shadowing one
another.

## Semantic Assessment

| Field | Type | Rule |
|---|---|---|
| `schema_version` | enum | New records emit `1.0.1`. `1.0.0` is accepted only as an explicit legacy-replay discriminator. |
| `assessment_id` | UUIDv7 | Primary identifier. |
| `candidate_id` | UUIDv7 | Required reference. |
| `provider_state` | enum | `live_openai`, `live_codex_subscription`, `recorded_fixture`, `failed`. Deterministic-only evaluations omit this record and are labeled at the API summary layer. |
| `requested_provider` | enum/null | Locally selected `fixture`, `openai`, or `codex_subscription`. New `1.0.1` assessments always populate it, including failures, and successful states require the exact mapped identity. Null or absence is accepted only when `schema_version=1.0.0` explicitly identifies legacy replay. A failed state does not erase which provider was attempted. |
| `requested_model` / `returned_model` | string/null | Direct live API successes require both identifiers. Subscription successes require the trusted local requested identifier and `returned_model=null`; fixture requests may be null. Current `1.0.1` failures for OpenAI or Codex subscription require the trusted local `requested_model` and always require `returned_model=null`; fixture failures may keep `requested_model=null`. The execution wrapper rejects either a requested-model mismatch or any failed result that attempts to assert a returned model before persistence. Explicit `1.0.0` records remain readable under their historical field shape but are never re-emitted unchanged. |
| `prompt_version` | string | Required. |
| `risk_score` | decimal/null | 0 through 1 for successful assessments; null on failure. |
| `categories` | ordered list[enum] | `persistent_instruction`, `privilege_escalation`, `tool_hijack`, `data_exfiltration`, `cross_task_contamination`, `self_reinforcement`, `secret_material`, `protected_namespace`, `concealed_instruction`, `benign_fact`, `benign_preference`, `ambiguous`. |
| `persistence_intent` | enum | `none`, `implicit`, `explicit`, `unknown`. |
| `authority_claim` | enum | `none`, `implied`, `explicit`, `unknown`. |
| `exfiltration_risk` | decimal/null | 0 through 1 on success; null on failure. |
| `tool_hijack_risk` | decimal/null | 0 through 1 on success; null on failure. |
| `cross_task_risk` | decimal/null | 0 through 1 on success; null on failure. |
| `secret_risk` | decimal/null | 0 through 1 on success; null on failure. |
| `rationale` | string/null | Bounded, pattern-sanitized rationale on success; null on failure. Routine UI detail replaces it with a fixed safe summary. |
| `recommended_disposition` | enum/null | `allow`, `redact`, `quarantine`, `block`; null on failure. |
| `sanitized_content_digest` | digest | SHA-256 binding to the sanitized candidate statement/content text. It is not a full candidate-payload digest or invocation nonce. |
| `cache_hit` | boolean | Reserved compatibility field; always `false` in the MVP because semantic reuse is disabled. |
| `latency_ms` | integer | Non-negative. |
| `failure` | object/null | Allow-listed safe class (`timeout`, `unavailable`, `refusal`, `incomplete`, `invalid_schema`, `invalid_response`, `internal_error`, `unsupported_auth`, `executable_drift`, `tool_activity`, `output_limit`, `process_exit`, `cleanup_failure`, or `cancelled`) and retryable flag. |
| `assessed_at` | UTC time | Required. |

Failed assessment records contain no fabricated scores and cannot be interpreted
as a clean finding. Their categories are empty, persistence and authority
signals are `unknown`, all risk fields are null, and rationale and recommended
disposition are null. These neutral failure invariants are identical in the
Pydantic model and public JSON Schema.

## Policy

| Field | Type | Rule |
|---|---|---|
| `engine_profile` | literal | `VC-POLICY-1`, selecting mandatory constitutional hard guards and match semantics. |
| `policy_id` | string | Lowercase slug, 3-64 characters. |
| `version` | semantic version | Required and immutable for one content digest. |
| `mode` | enum | `enforce` or `shadow`. |
| `default_action` | enum | `allow`, `redact`, `quarantine`, `block`. |
| `shadow_action` | enum | Applied only in shadow mode; default `allow`. |
| `protected_namespaces` | list[glob] | Validated namespace patterns. |
| `rules` | list[PolicyRule] | Ordered, unique IDs. |
| `failure_behavior` | FailurePolicy | Detector, semantic, ledger, and unknown-risk fallbacks. |
| `limits` | object | Candidate, evidence, stream, detector/semantic timeout, injection-budget, and TTL bounds. |
| `manual_review` | object | Globally required review actions and confirmation/reason/actor requirements. Kind and namespace review behavior is expressed by each rule's `manual_review_required` flag. |

The fields above form the validated `PolicyDocument`. A persisted
`PolicyRecord` additionally contains `content_digest` computed from the
validated canonical model and `activated_at` projected from `PolicyActivated`.
At startup, signed history must verify before the persisted active policy can be
used. If it does not, a configured or built-in `PolicyDocument` may be retained
only to render content-safe read-only status with `validation_state=invalid`;
it is not an active `PolicyRecord` and cannot authorize writes or injection.

`limits.injection_token_budget` is enforced as the maximum UTF-8 byte length of
the complete rendered context. This is a conservative token-count upper bound,
not an exact model-specific tokenizer. The renderer omits whole records that do
not fit and never truncates a record.

### Policy Rule

A rule has a unique ID, numeric priority, description, optional source classes,
namespace globs, kinds, detector categories, minimum severity, semantic score
threshold, sensitivity values, action, optional TTL, and manual-review flag.
`VC-POLICY-1` first applies versioned, non-configurable hard guards for
structural invalidity, raw credential material, and untrusted writes to
protected policy namespaces. For document rules, all populated match fields are
ANDed; list values and namespace patterns are ORed. Semantic predicates do not
match without a successful assessment. Remaining rules are evaluated by
`(priority, rule_id)` so equal priorities remain deterministic, and Pydantic
validation rejects duplicate rule IDs. The decision records the matched rule. Later
rules cannot silently weaken a terminal block from an earlier hard guard such
as credential material or protected namespaces.

## Policy Decision

| Field | Type | Rule |
|---|---|---|
| `decision_id` | UUIDv7 | Primary identifier. |
| `candidate_id` | UUIDv7 | Required reference. |
| `policy_id` / `policy_version` | string | Required active policy identity. |
| `policy_digest` | digest | Binds exact validated policy. |
| `matched_rule_id` | string/null | Null only for default action. |
| `mode` | enum | `enforce` or `shadow`. |
| `actual_action` | action | Applied outcome. |
| `would_have_action` | action | Computed enforcement outcome. |
| `shadow_mode` | boolean | Must equal mode semantics. |
| `reason_codes` | ordered list[string] | Stable safe codes. |
| `detector_result_ids` | ordered list[UUIDv7] | Exact inputs. |
| `semantic_assessment_id` | UUIDv7/null | Present only when used. |
| `decided_at` | UTC time | Required. |

In enforce mode, `actual_action == would_have_action`. In shadow mode,
`actual_action == policy.shadow_action` while `would_have_action` preserves the
enforcement result.

## Event Envelope

| Field | Type | Rule |
|---|---|---|
| `event_id` | UUIDv7 | Unique. |
| `stream_id` | string | Domain aggregate ID. |
| `sequence_number` | integer | Global contiguous sequence starting at 1. |
| `event_type` | enum | Published lifecycle name. |
| `occurred_at` | UTC time | Canonical signed-envelope format. |
| `actor` | object | Required `type` (`system`, `operator`, `codex`, `tool`, `agent`, `policy`, or `detector`) and bounded `id`. |
| `session_id` / `task_id` | string/null | Opaque origin values. |
| `source_class` | enum/null | Present when memory-related. |
| `memory_id` | UUIDv7/null | Present when memory-related. |
| `evidence_references` | ordered list[EventEvidenceReference] | `{evidence_id, digest}` references bound by the envelope. |
| `policy_id` / `policy_version` | string/null | Present for governed actions. |
| `detector_bundle_version` | string/null | Present for evaluation. |
| `semantic_model_identifier` | string/null | Trustworthy `returned_model` when present, otherwise the locally configured `requested_model`. For subscription execution this is the requested Codex model identifier and is not a remote-model attestation. Initial evaluation and rescan use the same derivation. |
| `payload` | object | Event-specific safe payload, independently digested. |
| `payload_digest` | digest | SHA-256 of exact payload bytes. |
| `previous_event_hash` | digest | Genesis is 64 zeroes. |
| `event_hash` | digest | SHA-256 of VC-CJ-1 signed body. |
| `canonicalization_algorithm` | literal | `VC-CJ-1`. |
| `digest_algorithm` | literal | `SHA-256`. |
| `signature_algorithm` | literal | `Ed25519`. |
| `signature` | padded Base64 | Ed25519 over raw event-hash bytes. |
| `signing_key_id` | string | Public-key digest-derived ID. |
| `schema_version` | string | Contract version, initially `1.0.0`. |

### Active event types

- `EvidenceCaptured`
- `EvidenceEvaluationCompleted`
- `EvidenceEvaluationFailed`
- `MemoryCandidateCreated`
- `DetectorVerdictRecorded`
- `SemanticAssessmentRecorded`
- `PolicyDecisionRecorded`
- `MemoryCommitted`
- `MemoryRedacted`
- `MemoryQuarantined`
- `MemoryBlocked`
- `MemoryApproved`
- `MemoryRevoked`
- `MemoryExpired`
- `PolicyActivated`
- `PolicyActivationRejected`
- `StreamStarted`
- `StreamAborted`
- `StreamCommitted`

`MemorySuperseded` and `LedgerCheckpointCreated` are reserved contract names but
need no speculative implementation until an active task requires them.

## Materialized Active Memory

| Field | Type | Rule |
|---|---|---|
| `memory_id` | UUIDv7 | Primary key, allocated when an eligible decision commits. |
| `commit_event_id` | UUIDv7 | Source `MemoryCommitted` event. |
| `candidate_id` | UUIDv7 | Source candidate. |
| `namespace` / `kind` | string | Typed classification. |
| `safe_statement` | string | Only injectible representation. |
| `source_class` | enum | Origin. |
| `policy_id` / `policy_version` | string | Admission policy. |
| `status` | enum | `active` or `redacted`; every other lifecycle state is absent from this projection. |
| `trust_decision` | enum | `allowed`, `redacted`, `manually_approved`, or `shadow_admitted`. |
| `actual_action` / `would_have_action` | action | Admission record retained in internal projection metadata. |
| `shadow_admitted` | boolean | True only for shadow admission. |
| `manual_approval_event_id` | UUIDv7/null | Stronger trust marker. |
| `committed_at` | UTC time | Required. |
| `expires_at` | UTC time/null | Derived TTL. |
| `last_event_sequence` | integer | Last affecting event. |

A row is eligible only when commit history exists; no later revoke,
supersession, or `MemoryExpired` event applies; its admission was
allow/redact/manual approve; and ledger/view verification succeeds. A due TTL
is converted into a signed `MemoryExpired` event by the lifecycle sweep before
session-start rendering. Replay never compares historical entries with the
current wall clock.

## Memory Inventory Record

The read-only inventory is a separate historical projection. It carries the
same identity, safe statement, classification, admission, and policy fields as
the active row plus status `active`, `redacted`, `revoked`, `superseded`, or
`expired` and the last affecting event. Revoked, superseded, and expired records
may appear in inventory and statistics but never in `active_memories` or
session-start injection.

## Targeted Rescan Lineage

A confirmed rescan addresses one active `memory_id`. It verifies that memory's
signed commit and original `MemoryCandidateCreated` event plus the latest signed
active policy. Current sanitization produces a fresh candidate with a new
`candidate_id`; its signed payload preserves the evidence references, links the
original candidate event in `durability_rationale`, records a bounded sanitized
operator reason, and identifies the rescan sanitizer version as its deterministic
extractor version.

The new candidate event, detector results, optional semantic assessment, and
policy decision append atomically. If an enforcement decision is `redact`,
`quarantine`, or `block`, the same transaction appends `MemoryRevoked` for the
original active memory and updates only that materialized record. An allow
decision leaves active memory unchanged but retains the complete signed rescan
decision history. Rescan is targeted; policy activation does not enumerate or
reevaluate every historical candidate automatically.

## Quarantine Projection

Quarantine rows hold candidate ID, decision ID, safe statement,
namespace, kind, source, risk categories, policy identity, quarantine event,
created time, and optional resolution event. Raw evidence is not copied.

State transitions:

```text
candidate -> quarantined -> approved -> active
                        \-> blocked
candidate -> blocked
candidate -> allowed/redacted -> active -> revoked
candidate -> shadow-allowed -> active(shadow) -> revoked
candidate -> allowed/redacted -> active -> expired
```

Every arrow is backed by one or more append-only events. Projection rows may be
replaced during replay; event rows may not.

## Stream

| Field | Type | Rule |
|---|---|---|
| `stream_id` | UUIDv7 | Primary identifier. |
| `state` | enum | `open`, `blocked`, `committing`, `committed`, `aborted`. |
| `metadata` | object | Candidate source/session/task/namespace hints. |
| `buffer_bytes` | integer | Bounded, non-negative. |
| `chunk_count` | integer | Bounded, non-negative. |
| `overlap_tail` | bytes | Internal bounded incremental-scan state. |
| `content_digest` | digest/null | Final buffer digest at terminal transition. |
| `started_at` / `updated_at` | UTC time | Required. |
| `terminal_reason` | string/null | Bounded safe reason code; operator-provided abort text is pattern-sanitized before event and projection persistence. |

Allowed transitions:

```text
open -> committing -> committed
open -> blocked
open -> aborted
committing -> aborted
```

All other transitions fail. Terminal states cannot append or commit.

## Signing Key Public Record

| Field | Type | Rule |
|---|---|---|
| `key_id` | string | `vc-ed25519-` plus the full lowercase SHA-256 hex digest of the raw public key. |
| `algorithm` | literal | `Ed25519`. |
| `public_key` | padded Base64 | Raw 32-byte public key. |
| `fingerprint` | digest | Full SHA-256 of raw public key. |
| `created_at` | UTC time | Required. |
| `status` | enum | `active`, `retired`, `revoked`. Only `active` used in MVP. |

The private key path is configuration, never database content or event payload.

## Expected Ledger Head

Normal local verification uses a restrictive, atomically replaced
`ledger-head.json` sidecar outside the SQLite file. It records schema version,
last sequence, last event hash, key ID, and an Ed25519 signature over the
canonical head record. The verifier may instead receive an explicitly exported
checkpoint. If neither is available, it can verify the supplied chain but must
report terminal completeness as unproven rather than fully verified. A failed
sidecar update after a database commit is a fail-safe health error; the daemon
does not silently bless the newer database head.

## SQLite Logical Schema

### Authoritative tables

- `schema_metadata(version, applied_at)`
- `event_payloads(payload_digest PRIMARY KEY, payload_bytes, byte_length,
  created_at)`
- `events(sequence_number PRIMARY KEY, event_id UNIQUE, stream_id, event_type,
  occurred_at, envelope_json, payload_digest, previous_event_hash, event_hash
  UNIQUE, signature, signing_key_id, schema_version)`
- `signing_keys_public(key_id PRIMARY KEY, algorithm, public_key,
  fingerprint, created_at, status)`

`events` is append-only at the application authorization boundary. SQLite
triggers reject update and delete in normal operation. Tamper tests use isolated
direct database access to prove verification, never production commands.

### Protected source records

- `evidence(evidence_id PRIMARY KEY, session_id, task_id, source_class,
  source_name, safe_excerpt, content_digest, protected_content,
  retention_state, captured_at, metadata_json)`
- `memory_candidates(candidate_id PRIMARY KEY, ..., creation_event_id UNIQUE)`
- `policies(policy_id, version, content_digest UNIQUE, validated_json,
  source_yaml, activation_event_id, PRIMARY KEY(policy_id, version))`

`evidence.protected_content` is a reserved schema column and is always null in
the MVP; no protected raw-evidence store is implemented.

### Rebuildable projections and operational indexes

- `active_memories(memory_id PRIMARY KEY, ..., last_event_sequence)`
- `memory_inventory(memory_id PRIMARY KEY, status, ..., last_event_sequence)`
- `quarantined_memories(candidate_id PRIMARY KEY, ..., resolution_event_id)`
- `pending_evidence(evidence_id PRIMARY KEY, state, sanitized_content,
  sanitized_content_digest, enqueued_at, attempts, next_attempt_at,
  last_error_code, failed_at, terminal_event_id)`
- `idempotency_keys(operation, idempotency_key, request_digest,
  response_json, created_at, PRIMARY KEY(operation, idempotency_key))`
- `streams(stream_id PRIMARY KEY, state, metadata_json, buffer_bytes,
  chunk_count, content_digest, started_at, updated_at, terminal_reason)`

Semantic assessment reuse is intentionally disabled. The compatibility field
`cache_hit` is always false; enabling a future cache requires signed,
provenance-sensitive binding so cached advisory output cannot silently change a
policy decision.

`pending_evidence` and `idempotency_keys` are operational indexes, not memory
trust authorities. A Control Room mutation reserves its idempotency key before
acting and writes the response afterward. If the process stops between those
steps, the indeterminate reservation is refused on retry. This safe-availability
tradeoff avoids blindly repeating a trust-changing operation but may require
local operator recovery.

## Invariants

1. Event sequence is unique and contiguous from 1 through the current maximum.
2. The first previous hash is all zeroes; every later previous hash equals the
   prior event hash.
3. Every event payload exists and reproduces `payload_digest`.
4. Every event hash and signature verify under the referenced public key.
5. An event envelope and payload are immutable through supported APIs.
6. One candidate has one final policy decision per evaluation attempt; a rescan
   creates a new attempt and events.
7. No quarantined or blocked candidate is present in active memory.
8. A revoked memory is absent after replay but its earlier events remain.
9. A stream has at most one terminal commit and no active memory before it.
10. Policy activation references a validated immutable policy digest.
11. Evidence and policy projection rows, including the single active-policy
    marker, reproduce their signed capture and activation events.
12. Semantic failure is not represented as a clean semantic assessment.
13. Telemetry and routine UI representations contain no recognized raw secret
    or original evidence bytes; sanitizer false negatives remain a documented
    residual risk.
14. A successful full verification has a valid expected-head sidecar or supplied
    checkpoint; without one, terminal completeness is explicitly unproven.
15. Hook acknowledgement implies a signed capture event and bounded pending row,
    not completed candidate evaluation.
16. A queued full sanitized body is deleted with successful outcomes or purged
    on terminal failure; a failed row contains no full sanitized body.
17. A terminal failed queue row exactly reproduces its signed capture and
    `EvidenceEvaluationFailed` identity, error class, attempt count, failure
    time, and terminal-event link; a recorded queue-integrity failure keeps the
    store unhealthy after restart.
